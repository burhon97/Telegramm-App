// { id: string, name: string, age: number, gender: string }

export type Contact = {
  id: string;
  name: string;
  age: number;
  jins: string;
};

const contactsList: Contact[] = [];

contactsList.map(contact => {
    return contact.
})

// return <>{contactsList.map(contact => contact.age)}</>
